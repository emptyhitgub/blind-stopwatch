# Blind Stopwatch

A stopwatch you can start and stop normally, or run in Blind Stop mode — the time stays hidden while it counts and only reveals itself the moment you stop it.

Single file, no build step, no dependencies. Open `index.html` in a browser, or serve this repo with GitHub Pages (Settings → Pages → Deploy from branch → `main` / root) for a live link.
